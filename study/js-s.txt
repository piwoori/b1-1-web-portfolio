1.  const menuToggle = document.querySelector('.menu-toggle');
- document
  - 현재 웹페이지를 의미
- querySelector()
  - HTML에 있는 조건에 맞는 첫 번째 요소를 js에서 조작하기 위해 가져오는 것
- querySelectorAll()
  - HTML에 있는 조건에 맞는 모든 요소를 js에서 조작하기 위해 가져오는 것
-  #은 id 선택자
- .은 클래스 선택자

2. const STATE
- STATE
  - 웹페이지의 현재 상태를 저장하는 객체
- projects: []
  - 깃허브 프로젝트를 받아오지 않았으니 빈 배열
- projectStatus: 'idle'
  - 아직 API 요청을 시작하지 않았다는 의미
- errorMessage: ''
  - 오류가 아직 없으니 빈 배열

3. let allProjects = [];
- API에서 받아온 전체 프로젝트 목록을 따로 저장하는 배열

4. const renderProjects
- renderProjects
  - 프로젝트 데이터를 받아서, HTML 프로젝트 카드로 만들어서, 화면에 출력하는 함수
- (projects)
  - 프로젝트들이 들어있는 배열
- projectList.innerHTML
  - projectList
    - const projectList = document.querySelector('#project-list');
  - innerHTML
    - 그 요소 내부의 HTML을 읽거나 바꿀 수 있는 속성
    - 여기서는 프로젝트 하나 당 HTML 카드 하나로 바꾸기 위해 사용
  - .map()
    - 배열의 각각의 값을 다른 값으로 변환해서 새로운 배열을 만드는 함수
  - const {...} = project
    - project 객체에서 필요한 값만 꺼내서 각각 변수로 만듦
    - 사용 이유: 객체 명을 계속해서 작성하는 것을 줄이고 코드 간결화를 위해
  - return `...`
    - 백틱 ``
      - 백틱으로 만든 문자열을 템플릿 리터럴이라 부름
      - 일반 문자열은 +로 연결해야 되지만 백틱을 이용하면 ${} 사용해 간결해짐
    - <p>${description ?? '프로젝트 설명이 없습니다.'}</p>
      - ?? (널 병합 연산자)
        - 왼쪽 값이 null 또는 undefined면 오른쪽 값을 대신 사용
  - .join('')
    - 배열의 요소들을 하나의 문자열로 합쳐주는 메서드
    - 여기서는 하나의 프로젝트 HTML 문자열들을 공백 없이 합침

5. const renderProjectFilters
- renderProjectFilters
  - 프로젝트들에서 사용된 언어 목록을 만드는 함수
- projects.map((project) => project.language)
  - 각 프로젝트 객체에서 language만 꺼냄
- .filter((language) => language !== null)
  - filter()
    - 조건을 만족하는 요소만 남기는 메서드
- ...new Set()
  - Set
    - 중복을 허용하지 않는 자료 구조
    - 배열이 아님
  - ... 
    - 스프레드 문법
    - 값을 펼치는 역할
- []
  - 괄호로 감싸서 배열로 만듦
- .sort()
  - 오름차순 정렬
- 최종적으로 중복과 null을 제거하고 정렬한 언어 목록이 languages에 들어감

6. const filterOptions = ['all', ...languages]
- ...
  - 스프레드 문법으로 languages를 펼침

7. filterOptions.forEach((language, index) ...)
- forEach()
  - 배열을 하나씩 순회하는 메서드
- const button = document.createElement('button')
  - createElement('button')
    - 원래 있던 버튼을 찾는 것이 아닌
    - 자바스크립트로 새로운 HTML <button> 요소를 생성
- 'all' ? '전체' : language
  - 삼항 연산자
    - 조건 ? 참일 때 값 : 거짓일 때 값 형식
- button.setAttribute('aria-pressed', String(index === 0))
  - index === 0
    - 첫 번째 버튼은 "전체" 버튼이고 이를 처음 선택된 상태로 두기 위함
- 첫 번째 버튼은 <button aria-pressed="true">
- 나머지 버튼은 <button aria-pressed="false">
- if (index === 0) {button.classList.add('active');}
  - 배열의 첫 요소 즉 "all"만 해당
  - classList.add('active')
    - 해당 HTML 요소에 CSS 클래스를 추가
- button.addEventListener('click', () => {})
  - 각각의 필터 버튼에 "클릭되면 이 코드를 실행해"라고 이벤트를 등록
  - const filterButtons = document.querySelectorAll('.project-filter-button');
    - 현재 만들어진 모든 필터 버튼을 가져옴
  - filterButtons.forEach((filterButton) => {})
    - filterButtons
      - filterButtons.forEach()에서 모든 필터 버튼을 하나씩 가리키는 변수
    - filterButton.classList.remove('active'); + filterButton.setAttribute('aria-pressed', 'false');
      - 모든 버튼을 돌면서 선택 상태를 싹 해제
  - button.classList.add('active'); + button.setAttribute('aria-pressed', 'true');
    - button
      - 지금 forEach에서 만들고 있고 사용자가 클릭한 해당 버튼
    - 방금 클릭한 button만 다시 선택 상태로 변경
  - const filteredProjects
    - 원본 allProjects를 바꾸지 않고 조건에 맞는 프로젝트만 담은 새로운 배열을 반환
    - language === 'all'
      - 전체 프로젝트를 그대로 사용
    - allProjects.filter((project) => project.language === language);
      - 선택한 언어와 같은 프로젝트만 filter()로 걸러냄
    - renderProjects(filteredProjects);
      - renderProjects()에 필터링된 배열을 다시 넘겨서 화면을 새로 그림

8. const renderProjectState
- API 요청 중인 화면을 의미
- projectList.innerHTML = '';
  - 시작하자마자 프로젝트 목록을 비움
- retryButton.classList.remove('visible');
  - 재시도 버튼 숨김
- if (STATE.projectStatus === 'loading')
  - 로딩 중일 때
  -  projectFilters.innerHTML = ''
    - 필터 비우고
  - projectStatus.textContent = '프로젝트를 불러오는 중...'
    - "프로젝트를 불러오는 중..." 표시
  - 리턴으로 함수 종료
- if (STATE.projectStatus === 'error')
  - 에러났을 때
  - projectFilters.innerHTML = ''
    - 필터 비우고
  - projectStatus.textContent = '프로젝트를 불러올 수 없습니다.'
    - 에러 메시지 표시
  - retryButton.classList.add('visible')
    - 재시도 버튼 표시
  - 리턴으로 함수 종료
- if (STATE.projectStatus === 'empty')
  - 프로젝트가 하나도 없을 때
  - projectFilters.innerHTML = ''
    - 필터 비우고
  - projectStatus.textContent = '표시할 프로젝트가 없습니다.'
    - 에러 메시지 표시
  - 리턴으로 함수 종료
- if (STATE.projectStatus === 'success')
  - 성공했을 때
  - projectStatus.textContent = ''
    - 상태 메시지 칸 비우고
  - renderProjectFilters(STATE.projects)
    - 프로젝트 언어 필터 버튼 생성
  - renderProjects(STATE.projects)
    - 프로젝트 카드 출력

9. const loadProjects
- 실제로 GitHub API에서 프로젝트 목록을 받아오는 함수
- async ()
  - 이 함수 안에서 시간이 걸리는 비동기 작업을 하겠다는 뜻
  - GitHub 서버에 요청을 보내고 응답을 기다려야 하니까 필요
- STATE.projectStatus = 'loading'
  - 프로젝트 상태를 loading으로 바꿈
- STATE.errorMessage = ''
  - 기존 에러 메시지는 비움
- renderProjectState()
  - 프로젝트를 불러오는 중... 표시 출력
- try 문
  - const response
    - GitHub API 요청을 보내고, 응답이 오면 그 응답을 response에 저장
    - await()
      - fetch 요청이 끝날 때까지 이 함수 안에서는 기다리게 하는 함수
    - fetch()
      - 서버에 HTTP 요청을 보내는 함수
  - if (!response.ok)
    - response.ok
      - 요청이 정상적으로 성공했는지 나타내는 값
    - throw new Error(...)
      - 일부러 에러 발생 시킴
      - 바로 아래 정상 코드로 계속 가지 않고 catch 쪽으로 넘어감
  - const projects = await response.json()
    - 응답 데이터를 JSON 형태에서 JavaScript 객체/배열로 변환
  - const filteredProjects = projects.filter((project) => !project.fork)
    - GitHub 저장소 중 fork한 저장소를 제외
  - allProjects = filteredProjects + STATE.projects = filteredProjects
    - API로 받아온 프로젝트 저장
    - 언어 필터링할 때 사용할 원본 전체 프로젝트 목록
  - STATE.projects = filteredProjects
    - API로 받아온 프로젝트 저장
    - 현재 화면 상태에서 사용할 프로젝트 데이터
  - STATE.projectStatus = STATE.projects.length === 0 ? 'empty' : 'success'
    - 개수에 따하 projectStatus 변경
  - renderProjectState()
    - 현재 상태에 맞게 다시 화면을 그림
- catch (error)
  - allProjects = [] + STATE.projects = []
    - 프로젝트 비움
  - STATE.projectStatus = 'error'
    - 상태를 error로 변경
  - STATE.errorMessage = error.message
    - 에러 메시지 저장
  - renderProjectState()
    - 현재 상태에 맞게 다시 화면을 그림
  - console.error('GitHub API Error:', error)
    - 에러 메시지 출력

10. const closeMobileMenu
- 모바일 메뉴를 닫고, 버튼 상태와 접근성 속성까지 닫힌 상태에 맞게 되돌리는 메소드
- 모바일 메뉴를 닫는 역할
- navMenu.classList.remove('active')
  - navMenu에서 active 클래스를 제거
  - <nav class="nav-menu active"> -> <nav class="nav-menu">
- menuToggle.setAttribute('aria-expanded', 'false')
  - 접근성용 속성
  - 스크린 리더 같은 보조기기에도 "현재 메뉴 닫혀 있음"이라고 알려줌
- menuToggle.setAttribute('aria-label', '메뉴 열기')
  - 버튼의 의미도 바꿈
- menuToggle.textContent = '☰'
  - 버튼 표시 변경

11. const toggleMobileMenu
- const isOpen = navMenu.classList.toggle('active')
  - toggle()
    - 클래스가 있으면 제거하고, 없으면 추가
  - 클래스만 변경하는 게 아니라 변경 후 해당 클래스가 존재하는지 true/false도 반환
- menuToggle.setAttribute('aria-expanded', String(isOpen))
  - isOpen이 boolean이기 때문에 String()으로 HTML 속성에 넣을 문자열 'true' 또는 'false'로 바꿈
- menuToggle.setAttribute('aria-label',isOpen ? '메뉴 닫기' : '메뉴 열기')
  - 열려있으면 메뉴 닫기, 아니면 메뉴 열기
- menuToggle.textContent = isOpen ? '✕' : '☰'
  - 열려있으면 전자, 아니면 후자

12. const handleNavLinkClick
- 내비게이션 메뉴에서 About, Projects, Contact 같은 링크를 클릭했을 때 해당 섹션으로 부드럽게 이동시키는 함수
- (event) => {}
  - 발생한 이벤트에 대한 정보를 담고 있는 객체
- event.preventDefault();
  - HTML 요소가 원래 하려고 했던 기본 동작을 막음
- const targetId = event.currentTarget.getAttribute('href')
  - HTML 요소가 원래 하려고 했던 기본 동작을 막는 것
  - event.currentTarget
    - 현재 이벤트 리스너가 달려 있는 요소
  - .getAttribute('href')
    - 해당 요소의 href 값을 가져옴
- const targetSection = document.querySelector(targetId);
  - 실제 targetId 섹션 찾음
- targetSection.scrollIntoView({behavior: 'smooth'})
  - scrollIntoView()
    - 해당 HTML 요소가 보이는 위치까지 화면을 스크롤해주는 메서드
  - behavior: 'smooth'
    - 순간 이동이 아닌 스르륵 이동
- closeMobileMenu()
  - 섹션으로 이동한 다음 모바일 메뉴도 닫음

13. const updateThemeButton
- 현재 테마에 맞게 버튼 아이콘과 설명을 바꾸는 매서드
- themeToggle.textContent = theme === 'dark' ? '☀️' : '🌙'
  - 현재 테마와 반대의 아이콘 저장
- themeToggle.setAttribute()도 동일

14. const initializeTheme
- 페이지를 처음 열었을 때 이전에 사용하던 테마를 복원하는 메서드
- const savedTheme = localStorage.getItem('theme')
  - localStorage
    - 브라우저에 값을 저장해두는 공간
- if (!savedTheme) {return;}
  - 저장된 테마가 없으면 그냥 함수 종료
- document.documentElement.setAttribute('data-theme', savedTheme)
  - document.documentElement
    - HTML 문서의 최상위 <html> 요소
    - <html>에 속성을 붙임(이미 존재하는 <HTML>에)
- updateThemeButton(savedTheme);
  - 현재 저장된 테마에 맞춰 버튼 아이콘까지 업데이트

15. const toggleTheme
- const currentTheme = document.documentElement.getAttribute('data-theme')
  - getAttribute()
    - 속성값을 가져오는 메서드
- const newTheme = currentTheme === 'dark' ? 'light' : 'dark'
  - 삼항 연산자로 현재 테마의 반대쪽 저장
- document.documentElement.setAttribute('data-theme', newTheme)
  - 새로운 테마로 변경
- localStorage.setItem('theme', newTheme)
  - 로컬 저장소에도 저장
  - 새로고침해도 데이터 유지
- updateThemeButton(newTheme)
  - 아이콘도 새 테마에 맞게 변경

16. const handleScroll
- 사용자가 얼마나 스크롤했는지 확인해서 HTML 요소의 클래스를 바꾸는 메서드
- 스크롤을 발생시키는 함수는 아님
- if (window.scrollY >= 60)
  - window.scrollY
    - 현재 브라우저 창을 나타내는 객체
    - 페이지 맨 위에서부터 세로로 얼마나 스크롤했는지를 픽셀(px) 단위로 알려줌
  - header.classList.add('scrolled')
    - header에 scrolled 클래스를 추가
- 그 외일 때는 
  - header.classList.remove('scrolled')
    - header에 scrolled 클래스를 제거
- if (window.scrollY >= 300)
  - scrollTopButton.classList.add('active')
    - 맨 위로 이동하는 버튼에 active를 추가
- 그 외일 때는
  - scrollTopButton.classList

17. const scrollToTop
- 페이지 맨 위로 부드럽게 이동시키는 메서드
- window.scrollTo()
  - 브라우저 화면을 지정한 위치로 스크롤시키는 메서드
  - top: 0
    - top은 세로 스크롤 위치, 0은 페이지의 가장 위를 의미
  - behavior: 'smooth'
    - 순간이동하지 않고 스르륵 이동

18. const revealObserver = new IntersectionObserver()
- IntersectionObserver()
  - 특정 요소가 화면에 들어왔는지 감시하는 기능
- (entries, observer) => {}
  - 변화가 생겼을 때 자동으로 실행해주는 콜백 함수
  - HTML 요소 그 자체라기보다는 그 요소의 관찰 결과를 담고 있는 객체
  - entries
    - 지금 관찰 중인 요소들의 상태 정보를 담은 배열
  - observer
    - 현재 사용 중인 IntersectionObserver 자기 자신
    - == const revealObserver = new IntersectionObserver(...)
  - entries.forEach((entry) => {})
    - entries 배열에 들어있는 각각의 요소 상태를 하나씩 확인
    - if (entry.isIntersecting)
      - entry.isIntersecting
        - 해당 요소가 현재 화면의 관찰 영역 안에 들어왔는지를 나타내는 boolean 값
      - entry.target.classList.add('visible')
        - entry.target
          - 지금 관찰하고 있는 실제 HTML 요소에 'visible' 클래스 추가
      - observer.unobserve(entry.target);
        - unobserve()
          - 관찰을 멈추는 메서드
- threshold: 0.2
  - threshold
    - 요소가 화면에 얼마나 들어왔을 때 들어왔다고 판단할지 정하는 값

19. const initializeScrollAnimation\
- 스크롤해서 요소가 화면에 들어오면 한 번 등장 애니메이션을 실행하는 메서드
- revealElements.forEach((element) => {})
  - revealElements의 요소들을 하나씩 꺼냄
  - revealObserver.observe(element);
    - observe()로 감시 시작 등록

20. const isValidEmail = (email) => {}
- 전달받은 문자열이 이메일 형식처럼 생겼는지 검사하는 메서드
- const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  - 정규표현식
    - 문자열이 특정 패턴을 만족하는지 검사할 때 쓰는 문법
- return emailPattern.test(email)
  - test()
    - 문자열이 이 정규표현식 패턴과 맞는지 검사하는 메서드
    - boolean으로 반환

21. const validateForm
- 폼을 제출할 때 이름 / 이메일 / 메시지를 한 번에 검사해서 제출 가능한 상태인지 true/false로 반환
- let isValid = true
  - 문제 없다고 가정
  - 후에 값을 변경해야하므로 let 사용
- .textContent = ''
  - 기존에 화면에 표시되어 있던 메시지를 전부 초기화
- if (nameInput.value.trim() === '')
  - nameInput
    - const nameInput = document.querySelector('#name')의 이름 입력창
  - nameInput.value
    - 사용자가 입력창에 실제로 입력한 값
  - .trim()
    - 문자열 앞뒤의 공백 제거
  - nameError.textContent = '이름을 입력해주세요.'
    - 에러 메시지 보여주기
  - isValid = false
    - 전체 폼을 유효하지 않은 상태로 변경
- if (emailInput.value.trim() === '')
  - 위와 동일
  - emailError.textContent = '이메일을 입력해주세요.'
    - 에러 메시지 보여주기
  - isValid = false
    - 전체 폼을 유효하지 않은 상태로 변경
- else if (!isValidEmail(emailInput.value.trim()))
  - 위와 동일
- if (messageInput.value.trim() === '')
  - 위와 동일
- return isValid
  - 최종 결과를 반환

22. const handleContactSubmit = (event) => {}
- Contact 폼에서 제출 버튼을 눌렀을 때 실행되는 메서드
- event.preventDefault()
  - <form>의 기본 동작을 막음
  - event
    - 발생한 이벤트 정보
  - preventDefault()
    - HTML의 기본 동작을 막는 것
- const isValid = validateForm()
  - validateForm()의 결과값 저장
- if (!isValid)
  - 유효성 검사에 실패하면 여기서 제출 처리를 끝내림
- formSuccess.textContent = '메시지가 정상적으로 작성되었습니다.'
  - 성공 메시지를 화면에 표시
- contactForm.reset()
  - reset()
    - 폼의 입력값들을 초기화하는 메서드
  - 입력창이 다시 비움

23. const validateNameInput
- 입력 중 실시간 검사하는 메서드
- 사용자가 타이핑하는 동안 바로 에러 메시지를 갱신

24. const validateEmailInput
- const email = emailInput.value.trim()
  - 사용자가 입력한 값을 가져오고 앞뒤 공백 제거한 걸 email에 저장
- if (email === '')
  - 비어 있을 때
- else if (!isValidEmail(email))
  - 값은 있는데 형식 틀릴 때
- 형식 정상일 때
  - emailError.textContent = ''
    - 에러 메시지 제거

25. const validateMessageInput
- 입력 중 실시간 검사하는 메서드
- 사용자가 타이핑하는 동안 바로 에러 메시지를 갱신

26. menuToggle.addEventListener('click', toggleMobileMenu)
- 메뉴 버튼 클릭
  - toggleMobileMenu() 실행
- navLinks.forEach((link) => {link.addEventListener('click', handleNavLinkClick);})
  - navLinks.forEach((link))
    - navLinks는 여러 개니까 하나씩 돌면서 각각에 클릭 이벤트를 붙임

27. themeToggle.addEventListener('click', toggleTheme) ~
- 테마 버튼 클릭
  - toggleTheme() 실행
- 함수들이 실제로 언제 실행될지 연결
- messageInput.addEventListener('input', validateMessageInput)까지 동일

28. Initialization
- 페이지가 처음 실행될 때 필요한 초기 작업들을 실행하는 부분
- initializeTheme()
  - 페이지를 처음 열었을 때 저장된 테마가 있는지 확인
- initializeScrollAnimation()
  - .reveal 요소들을 IntersectionObserver에 등록
  - 스크롤 등장 애니메이션을 사용할 준비
- loadProjects()
  - GitHub API에서 프로젝트를 처음 불러옴
- Event Listener와 Initialization의 차이
  - Event Listener: 나중에 클릭되면 실행
  - Initialization: 지금 바로 실행